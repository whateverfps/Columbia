COLUMBIA AI — V15 FUTURE FABRIC + X-RAY LAB

Built from V14 without removing the Chief orb, page-specific Paul's AI Vision tools, Vision Hub, Live Demo, finance scenario behavior, scrolling fixes, or existing role worlds.

V15 additions:
- Wallpaper-inspired ambient intelligence restored across the HTML suite.
- Role-specific visual language instead of one generic motion treatment:
  Enterprise = living digital building / twin
  Executive = decision constellation
  Project = blueprint scanner / constraint hotspot
  Preconstruction = separated drawing/spec/bid layers
  Finance = perspective forecast geometry
  People = workforce constellation
  IT = governed data fabric / policy packets
  Studio = idea forge
  Journey = transformation corridor
  Business Case = value field
- X-Ray Lab added beneath the existing X-Ray workspace:
  Access Map + animated data lineage
  Source health
  Why-access explanation
  Compare Roles permission view
  Evidence Inspector
  Audit + Controls view
  Export illustrative audit JSON packet
  Request Permission Simulator with allowed/blocked examples
- Vision Hub desktop scrolling is explicitly forced on long content.

All X-Ray data is illustrative concept behavior and does not expose private model chain-of-thought.
