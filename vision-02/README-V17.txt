V17 — X-Ray Layout Repair

This release keeps every V16 feature and fixes the X-Ray display problem shown in the supplied screenshots.

Changes:
- Removed sticky X-Ray page header that could cover source/evidence rows while scrolling.
- Removed sticky X-Ray Lab tab bar that could cover the top of the lineage map.
- X-Ray workspace is now the single scroll container with a stable scrollbar gutter.
- X-Ray Lab tabs remain together in a dedicated non-overlapping header.
- Increased map canvas height slightly so labels and routes remain fully visible.
- Preserved all V16 dashboards, Vision Hub, Live Demo, Chief orb, role-specific Paul’s AI Vision experiences, Finance Time Machine, Future Fabric, and System X-Ray functionality.
